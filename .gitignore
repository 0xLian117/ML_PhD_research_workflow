# ══════════════════════════════════════════════════
# ML PhD Research Workflow — .gitignore
# ══════════════════════════════════════════════════

# ── Code projects (each is its own git repo) ──
# Add your project directories here:
# code/project_a/
# code/project_b/
code/*/

# ── Project-specific context (customize locally) ──
CLAUDE.md
context/

# ── Experiment results (large binary files) ──
results/**/*.pt
results/**/*.pth
results/**/*.onnx
results/**/*.tar.gz
results/**/*.zip
results/**/train.log

# ── Python ──
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
*.egg
dist/
build/
.eggs/

# ── Virtual environments ──
.venv/
venv/
env/

# ── IDE ──
.vscode/
.idea/
*.swp
*.swo
*~

# ── Secrets ──
.env
.env.*
*.key
*credentials*
*secret*

# ── ML artifacts (if any leak to root) ──
*.pt
*.pth
*.onnx
*.h5
*.pkl
wandb/

# ── LaTeX build artifacts ──
*.aux
*.bbl
*.blg
*.fdb_latexmk
*.fls
*.log
*.out
*.synctex.gz
*.toc
*.nav
*.snm
*.vrb

# ── Project-specific logs/plans/reviews (local only) ──
quality_reports/**/*.md
reports/**/*.md

# ── Claude Code local/session state ──
.claude/settings.local.json
.claude/projects/
.claude/hooks/.response_counter

# ── OS ──
.DS_Store
Thumbs.db
