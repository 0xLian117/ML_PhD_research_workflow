# ══════════════════════════════════════════════════
# delta_research workflow repo .gitignore
# ══════════════════════════════════════════════════

# ── Code projects (each is its own git repo) ──
# Ignore ALL contents, only track the code/ directory structure itself
code/delta_data_sdk/
code/delta_alpha/
code/delta_learn/
code/delta_llm/

# ── Experiment results (large binary files) ──
# Track directory structure (.gitkeep) but not actual results
results/**/*.pt
results/**/*.pth
results/**/*.onnx
results/**/*.tar.gz
results/**/*.zip
results/**/train.log

# ── Python ──
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
*.egg
dist/
build/
.eggs/

# ── Virtual environments ──
.venv/
venv/
env/

# ── IDE ──
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# ── Secrets ──
.env
.env.*
*.key
*credentials*
*secret*

# ── ML artifacts (if any leak to root) ──
*.pt
*.pth
*.onnx
*.h5
*.pkl
wandb/

# ── LaTeX build artifacts ──
*.aux
*.bbl
*.blg
*.fdb_latexmk
*.fls
*.log
*.out
*.synctex.gz
*.toc
*.nav
*.snm
*.vrb

# ── Claude Code local/session state ──
.claude/settings.local.json
.claude/projects/
.claude/hooks/.response_counter

# ── OS ──
.DS_Store
Thumbs.db
